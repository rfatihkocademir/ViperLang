@effect(os)
pub fn read_user(user_id) -> str {
    panic("boom")
    ret os_env(user_id)
}

fn render(user_id) -> str {
    ret read_user(user_id)
}

fn route(user_id) -> str {
    ret read_user(user_id)
}
